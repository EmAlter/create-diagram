package com.emalter.creatediagram.logic;

public enum RecipeType {
    STANDARD, // Every input is consumed
    MIXED, // Some inputs are consumed, some are not
    INFINITE // No inputs are consumed
}

